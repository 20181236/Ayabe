using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BehaviorTree : BehaviorTreeBase
{
    public override void BuildBehaviorTree()
    {
        // 이 안에서 실제 행동 트리를 구성하는 코드를 작성합니다.
        // 예: rootNode = new SelectorNode(...);
        //     rootNode.AddChild(new SequenceNode(...));
    }

}